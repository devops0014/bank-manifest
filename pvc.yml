apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-3
spec:
  storageClassName: manual
  volumeName: pv-2
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
